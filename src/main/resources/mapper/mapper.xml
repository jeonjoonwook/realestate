<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.joonwook.realestate.Mapper.TestMapper">
    <select id="test" resultType="com.joonwook.realestate.dto.Test">
        select apt_id,
               apt_nm
        from apt
    </select>

</mapper>